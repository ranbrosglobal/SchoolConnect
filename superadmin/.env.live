# Live mode: talks to the SQLite backend (sc_backend) instead of the in-browser mock.
# Loaded by: npm run dev:superadmin  (vite --mode live)
VITE_APP_MODE=live
VITE_API_URL=http://13.205.212.64:3001
