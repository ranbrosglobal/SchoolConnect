# Live mode: talks to the SQLite backend (sc_backend) instead of the in-browser mock.
# Loaded by: npm run dev -- --mode live
VITE_APP_MODE=live
# When using vite dev proxy, API calls to /api/* are forwarded to the backend
# No need to set VITE_API_URL - the proxy handles routing
