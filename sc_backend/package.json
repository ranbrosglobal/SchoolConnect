{
  "name": "sc_backend",
  "version": "1.0.0",
  "private": true,
  "description": "SQLite backend for SchoolConnect web consoles",
  "type": "module",
  "scripts": {
    "start": "node --experimental-sqlite server.js",
    "reset": "node --experimental-sqlite src/index.js --reset"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
