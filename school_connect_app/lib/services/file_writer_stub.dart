import 'dart:typed_data';

/// Web stub: never called (web paths use `triggerDownload` instead).
Future<String> writeBytesToDir(
    String dirPath, String filename, Uint8List bytes) async {
  return '';
}
